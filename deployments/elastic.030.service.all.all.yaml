apiVersion: v1
kind: Service
metadata:
  name: elastic
  namespace: TARGET_K8S_NAMESPACE
  labels:
    app: elastic-search
spec:
  selector:
    app: elastic-search
  ports:
    - name: http
      port: 9200
    - name: transport
      port: 9300
      protocol: TCP